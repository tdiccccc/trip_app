services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace:cached
    working_dir: /workspace
    ports:
      - "3900:3900"
      - "8900:8900"
    command: sleep infinity
