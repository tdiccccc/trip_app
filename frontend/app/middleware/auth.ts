export default defineNuxtRouteMiddleware((to) => {
  // TODO: useAuth composable で認証状態を確認する
  // const { isAuthenticated } = useAuth()
  // if (!isAuthenticated.value) {
  //   return navigateTo('/login')
  // }
})
